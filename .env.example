# Frontend environment example
# Copy this file to `.env.local` or `.env.production` and set the base URL
# for your backend API.

# Local development backend (optional override of the default http://localhost:5000)
# VITE_API_BASE_URL=http://localhost:5000

# Production backend URL (e.g. Render/Railway deployment)
VITE_API_BASE_URL=https://your-backend-service.example.com
