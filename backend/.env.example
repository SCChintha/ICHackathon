# Backend environment example
# Copy this file to `.env` in the backend folder and fill in values.

# MongoDB connection string, including username and password.
# Example (replace placeholders and cluster info):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/ICHackathon?retryWrites=true&w=majority

MONGO_URI=
